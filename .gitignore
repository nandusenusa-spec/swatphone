# v0 sandbox internal files
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts
.snowflake/
.v0-trash/
next.user-config.*

# Secrets & environment (never commit real values)
.env
.env.local
.env.backup
.env.demo
.env.production
.env*.local
.env.*
!.env.example
!.env.local.example
!.env.backup.example
!.env.demo.example
# Copia local rellenada desde vercel-env.template (no commitear)
vercel-env.local

# Dependencies & build output
node_modules/
.next/
dist/
build/
*.tsbuildinfo

# Tooling / OS
.DS_Store
.vercel/

# DB backups locales (pueden contener datos sensibles)
backups/*.sql
backups/*.dump
backups/*.tar
!backups/.gitkeep
